A little parser and React UI which allows copy-paste of complex Psalm error messages and gives a visual breakdown of the differences.

## Build
```sh
pnpm install
pnpm build
```
## Development
```sh
pnpm install
pnpm start
```
